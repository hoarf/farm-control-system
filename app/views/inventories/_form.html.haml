= simple_form_for(@inventory) do |f|
  %p= f.error_notification
  .container
    .row
      .col-md-4
        %h3 Dados do Estoque
        %hr
        = f.input :item, autofocus: true
        = f.input :unit
        = f.input :description
        %hr
        = f.button :submit, class: "btn-primary"
