.nested-fields
  .container
    .row
      .col-md-4
        = f.input :amount
        = f.association :credit
        = f.association :debit
        = f.association :partner
        = f.input :evidence
      .col-md-4#entry
        = f.simple_fields_for :entry do |e|
          =render 'entry_fields', f: e
        .links
  %i.fa.fa-remove.fa-fw
  = link_to_remove_association "Remover esta movimentação", f
  %hr
